[sqlfluff]
dialect = postgres
max_line_length = 80
# RF04: column names like 'type', 'state', 'zone' are reserved words in SQL
# standard but valid identifiers in PostgreSQL — quoting them would break
# compatibility with existing JPA entity mappings.
# LT05: long lines in SQL comments and multi-value INSERT rows are acceptable.
exclude_rules = RF04, LT05
